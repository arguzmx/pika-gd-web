import { SearchFieldsDirective } from './search-fields.directive';

describe('SearchFieldsDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchFieldsDirective();
    expect(directive).toBeTruthy();
  });
});
