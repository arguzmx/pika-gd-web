import { AclMostrarDirective } from './acl-mostrar.directive';

describe('AclMostrarDirective', () => {
  it('should create an instance', () => {
    const directive = new AclMostrarDirective();
    expect(directive).toBeTruthy();
  });
});
